import React from 'react';

const b = 3

function c(param: object) {
}

export const Function = (other: object) => {
  return (
    <div>
      {
        ((param) => {
          return 1
        })({
          s: () => {
          }

        }) === 1 ? (<div></div>) : (<div></div>)


      }
      {1 === Math.random() ? (
        <div></div>
      ) : (
        <></>
      )}
    </div>
  );
};