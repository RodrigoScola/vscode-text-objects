import React from 'react';


const arr = [1, 1, 2]

export const Function = () => {
  return (
    <div>
      {1 === Math.random() ? (
        <div></div>
      ) : (
        <></>
      )}
    </div>
  );
};